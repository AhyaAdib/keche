using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Pathfinding;

public class playerMoveController : MonoBehaviour
{/*
    public playerMovement[] playerMovements;
    public AIPath[] aIPaths;
    public playerBotScript[] playerBot;
    public int currentIndex;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
         if(Input.GetKeyDown(KeyCode.Q))
        {
            if(currentIndex == 0)
            {
                playerMovements[playerMovements.Length - 1].enabled = false;
                playerMovements[currentIndex].enabled = true;
                aIPaths[aIPaths.Length - 1].canMove = true;
                playerBot[aIPaths.Length - 1].enabled = true;
                aIPaths[currentIndex].canMove = false;
                playerBot[currentIndex].enabled = false;

            } else if(currentIndex >= 1)
            {
                playerMovements[currentIndex - 1].enabled = false;
                playerMovements[currentIndex].enabled = true;
                aIPaths[currentIndex - 1].canMove = true;
                playerBot[currentIndex - 1].enabled = true;
                playerBot[currentIndex].enabled = false;
                aIPaths[currentIndex].canMove = false;
            }
        }
    }*/
}
