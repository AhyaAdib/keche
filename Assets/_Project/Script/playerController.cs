using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Pathfinding;

public class playerController : MonoBehaviour
{
    /*
    public playerMovement playerMovements;
    public AIPath aIPaths;
    public playerBotScript playerBot;
    public bool inMainPlayer;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if(inMainPlayer)
        {
            playerMovements.enabled = true;
            playerBot.enabled = false;
            aIPaths.canMove = false;
        } else {
            playerMovements.enabled = false;
            playerBot.enabled = true;
            aIPaths.canMove = true;
        }
    }*/
}
